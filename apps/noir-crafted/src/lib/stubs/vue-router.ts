/**
 * Stub for vue-router - not available in SvelteKit
 * This prevents Vite from trying to resolve vue-router at build time
 */
export const RouterLink = function StubRouterLink(props: any) {
  return props;
};
